# oxfmt (howells-check formatting) reads .prettierignore by default.
# components/ui holds unmodified stock shadcn components in shadcn's own style;
# they are regenerated by `shadcn add`, so we don't reformat them to house style.
components/ui/
