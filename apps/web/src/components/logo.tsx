import { Pilcrow } from "lucide-react";

const Logo = () => {
  return (
    <Pilcrow
      className="size-7 text-zinc-600 dark:text-zinc-400 scale-x-[-1]"
      strokeWidth={1.5}
    />
  );
};

export default Logo;
