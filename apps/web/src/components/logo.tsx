import { Pilcrow } from "lucide-react";

const Logo = () => {
  return (
    <Pilcrow
      className="size-7 scale-x-[-1] text-zinc-600 dark:text-zinc-400"
      strokeWidth={1.5}
    />
  );
};

export default Logo;
