---
"@patternmode/channel": patch
---

Draw a range's dimmed veil on the track instead of clipping the control. Chromium ignores calc() values for overflow-clip-margin, so the clip cut the pill's rounded ends off flush with the thumbs at both endpoints.
