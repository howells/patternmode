#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Validate package.json alignment
echo "🔍 Validating package.json alignment..."
pnpm validate-packages

# Run existing linting
echo "🔧 Running linters..."
pnpm lint