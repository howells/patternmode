"use client";

import { Toolbar as BaseToolbar } from "@base-ui-components/react/toolbar";

// Backwards-compatible alias exposing the unstyled primitive Root
export const ToolbarRoot = BaseToolbar.Root;
