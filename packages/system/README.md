# @patternmode/system

Shared Patternmode primitives for package authors.

```ts
import {
  getObjectSizingStyle,
  getResponsiveClasses,
  getSizeVariableStyle,
  joinClassNames,
} from "@patternmode/system";
```

Use it for semantic component sizing, responsive object props, object-fit media sizing, CSS size conversion, and class name composition.
