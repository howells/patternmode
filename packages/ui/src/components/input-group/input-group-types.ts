import type { ComponentSize } from "../../lib/size";

/** Size options for InputGroup components. */
export type InputGroupSize = ComponentSize;

/** Alignment options for InputGroupAddon. */
export type InputGroupAddonAlign =
  | "inline-start"
  | "inline-end"
  | "block-start"
  | "block-end";

/** Variant options for InputGroupAddon. */
export type InputGroupAddonVariant = "default" | "muted";
