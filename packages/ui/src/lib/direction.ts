/**
 * Flex direction types.
 * Used by `Flex` and `Stack` via the `direction` prop.
 * Note: Stack uses `"column"` instead of `"col"` for consistency with CSS.
 */
export type FlexDirection = "row" | "column" | "row-reverse" | "column-reverse";
