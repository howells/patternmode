/**
 * Flexbox main-axis alignment types.
 * Used by `Flex`, `Stack`, `HStack`, `VStack`, and `Grid` via the `justify` prop.
 */
export type FlexJustify =
  | "flex-start"
  | "flex-end"
  | "center"
  | "space-between"
  | "space-around"
  | "space-evenly";
