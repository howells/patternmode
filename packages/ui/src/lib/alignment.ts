/**
 * Flexbox cross-axis alignment types.
 * Used by `Flex`, `Stack`, `HStack`, `VStack`, and `Grid` via the `align` prop.
 */
export type FlexAlign =
  | "stretch"
  | "flex-start"
  | "flex-end"
  | "center"
  | "baseline";
